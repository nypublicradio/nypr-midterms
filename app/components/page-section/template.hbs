{{yield (hash
  tease=(component 'page-section/tease' isOpen=isOpen)
  body=(component 'page-section/body' isOpen=isOpen)
  toggleOpen=(action toggleOpen)
  isOpen=isOpen
)}}

<div class="page-section__footer">
  <button class="page-section__toggle" {{action toggleOpen}}>
    {{nypr-svg icon="down-arrow"}}
  </button>
</div>
